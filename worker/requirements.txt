# Install from inside this directory -- pip resolves the relative path below
# against the working directory, not against this file:
#     cd worker && pip install -r requirements.txt
-e ../shared

aio-pika==9.4.3
pandas==2.2.3
# Required by pandas for Parquet; not imported directly.
pyarrow==17.0.0
sqlglot==25.24.5

# Pulled in through the shared package, which declares only lower bounds
# because it is a library. This service is what actually runs, so it pins the
# versions it was tested against.
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
pydantic==2.13.4
pydantic-settings==2.15.0
